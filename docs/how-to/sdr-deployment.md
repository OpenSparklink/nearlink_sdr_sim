# SDR E310 部署与 MAC 层完善规划

本文档概述将 NearLink SDR 从纯仿真环境迁移到 USRP E310 硬件的路径，
并分析 MAC 层协议栈中尚需补全的基础设施。

## 当前完成度

### 物理层 (第 6 章)

已完成全部核心功能:

| 功能 | 模块 | 标准章节 |
|------|------|----------|
| GFSK 调制解调 | `phy/gfsk.py` | 6.2.1.1 |
| BPSK/QPSK 调制解调 | `phy/psk.py` | 6.2.1.2 |
| 帧结构 FT1-FT4 | `phy/frame.py` | 6.3 |
| 前导码 | `phy/preamble.py` | 6.2.2 |
| 同步信号 1-6 | `phy/sync_sequence.py` | 6.2.3 |
| 导频 | `phy/pilot.py` | 6.7 |
| 控制信息 A/B 组 | `phy/control_info.py` | 6.4 |
| CRC-12/24A/24B/32 | `common/crc.py` | 6.9 |
| Polar 编解码 | `common/polar.py` | 6.10.1 |
| 码块分割 | `common/code_block_seg.py` | 6.10.2 |
| 跳频序列 | `phy/freq_hopping.py` | 6.10.3 |
| 信道比特加扰 | `common/scrambler.py` | 6.10.4 |
| MCS 表与速率匹配 | `common/mcs.py` | 6.10.5/6 |
| BCH 编码 | `common/bch.py` | 6.11 |
| TX/RX 流水线 | `phy/tx_pipeline.py`, `phy/rx_pipeline.py` | 6.10 |
| 测量信号 1/2 | `phy/measurement.py` | 6.2.4 |
| 测量帧类型 1-4 + UWB | `phy/measurement_frame.py` | 6.3.6-11 |
| USRP E310 接口 | `phy/usrp.py` | - |
| 信道模型 + 均衡器 | `phy/channel.py`, `phy/equalizer.py` | - |

### MAC 层 (第 7 章)

| 功能 | 模块 | 标准章节 |
|------|------|----------|
| 控制面/数据面/复用帧结构 | `mac/frame.py` | 7.3 |
| 信令注册表 (128+ 类型) | `mac/signaling.py` | 7.3.2 全覆盖 |
| 链路控制信令 (135 类) | `mac/link_control.py` | 7.3.2.2-114 |
| 功率控制 | `mac/power_control.py` | 7.2.13 |
| 广播帧 + 子信息 | `mac/broadcast.py` | 7.1.4 |
| 链路管理状态机 | `mac/link_manager.py` | 7.1/7.2 |
| 配对信令 | `mac/security.py` | 9.2 |
| 加密模块 | `mac/crypto.py` | 9.3/9.4 |

### 仿真 (9 阶段)

| 阶段 | 内容 |
|------|------|
| Phase 1 | GFSK/BPSK/QPSK 未编码 BER |
| Phase 2 | Polar 编码 BER/FER |
| Phase 3 | 帧级导频 BER/FER |
| Phase 4 | 多径衰落 + 均衡器性能 |
| Phase 5 | 频率选择性信道 |
| Phase 6 | 全 TX/RX 流水线 |
| Phase 7 | 多径 + 频偏集成 |
| Phase 8 | Pipeline 全帧类型对比 |
| Phase 9 | MAC 帧端到端 (信令/数据/复用) |

---

## MAC 层待完善基础设施

以下功能在标准中有明确定义，但当前实现中缺失或不完整。

### 1. 时隙与调度子系统

**标准依据**: 6.3.1, 7.1.4.3, 7.1.4.4

SLE 的时间调度基于两级时隙体系:

- **系统基础时隙**: 125 us 固定粒度，30 位递增计数器
- **系统调度时隙**: 可配置 25/50/75/100/125 us，30 位计数器

需要实现:

```
mac/scheduler.py:
  - SystemClock        # 系统时钟管理，基础/调度时隙计数
  - EventGroup         # 事件组: 周期、偏移、事件个数
  - EventScheduler     # 事件调度: 先发/后发窗口计算
  - SlotAllocator      # 时隙分配器
```

### 2. 休眠与唤醒管理

**标准依据**: 7.2.14, 8.2.2.4-5

SLE 低功耗运行的核心机制:

- 活动状态 / 低功耗状态切换
- 睡眠时钟精度协商 (8 级: 0~500 ppm)
- 锚点事件唤醒时刻计算

需要实现:

```
mac/sleep.py:
  - SleepClockAccuracy  # 睡眠时钟精度枚举与计算
  - PowerStateManager   # 活动/低功耗状态管理
  - WakeupCalculator    # 唤醒时刻计算 (考虑时钟漂移)
```

### 3. 广播帧子信息 (缺失项)

**标准依据**: 7.1.4.1-11

已实现 3 个 (7.1.4.3/8/9)，缺失 8 个:

| 编号 | 名称 | 优先级 |
|------|------|--------|
| 7.1.4.1 | 扩展广播帧资源配置信息 | 高 |
| 7.1.4.2 | 发现接入资源配置信息 | 高 |
| 7.1.4.4 | 接入基本信息 | 高 |
| 7.1.4.5 | 接入请求信息 | 高 |
| 7.1.4.6 | 接入响应信息 | 高 |
| 7.1.4.7 | 启动系统管理帧信息 | 中 |
| 7.1.4.10 | 非链接态窄带跳频测量配置 | 低 |
| 7.1.4.11 | 非链接态超宽带脉冲测量配置 | 低 |

### 4. 控制面流程实现

**标准依据**: 7.2.1-14

`link_manager.py` 已实现状态机框架，但以下控制面流程需要完善:

| 流程 | 标准 | 说明 |
|------|------|------|
| 收发间隔参数更新 | 7.2.1 | 6 种场景，涉及 G/T 角色交互 |
| 多级收发间隔更新 | 7.2.2 | 多间隔级别管理 |
| 特性交互 | 7.2.4 | 能力协商 |
| 版本交互 | 7.2.5 | 规格版本、公司标识符 |
| 数据长度更新 | 7.2.6 | 异步链路 PDU 长度协商 |
| 信道质量上报 | 7.2.7 | G→T 上报指示，T 回复信道状态 |
| 跳频频点表/地图更新 | 7.2.8/9 | 动态频率管理 |
| CRC 切换 | 7.2.11 | 24/32 位 CRC 动态切换 |
| 物理层类型更新 | 7.2.12 | 调制方式动态切换 |
| 休眠与唤醒 | 7.2.14 | 低功耗状态管理 |

### 5. 接入流程

**标准依据**: 7.1.3

设备发现和连接建立的完整流程:

```
mac/access.py:
  - DiscoveryManager   # 广播/扫描/查询
  - AccessManager      # 接入请求/响应/确认
  - RoleNegotiator     # G/T 角色协商
```

---

## SDR E310 迁移规划

### 前置条件

1. USRP E310 硬件就绪，UHD 驱动已安装
2. 宿主机与 E310 网络连通 (或直接在 E310 ARM 上运行)
3. SLE 2.4 GHz 频段射频校准完成

### 阶段一: 硬件验证

**目标**: 验证 `phy/usrp.py` 在真实硬件上的基本功能

```
scripts/hw_verify.py:
  1. 初始化 USRPDevice(use_mock=False)
  2. 验证频率设置 (2402-2480 MHz)
  3. 验证增益控制 (TX/RX)
  4. 发射单载波 → 频谱仪确认
  5. 回环测试 (TX→RX, 外部衰减器/线缆)
```

关键参数:
- 采样率: 1 MHz (GFSK) 或 2 MHz (QPSK)
- 中心频率: 2440 MHz (信道 19)
- TX 增益: 20 dB (初始低功率)
- RX 增益: 40 dB

### 阶段二: 物理层回环

**目标**: TX pipeline → 空口/线缆 → RX pipeline 回环验证

```
scripts/phy_loopback.py:
  1. 构造 FT2 帧 (GFSK, MCS=0)
  2. mac_to_iq() 生成 IQ 样本
  3. SLETransceiver.transmit_iq() 发射
  4. SLETransceiver.receive_iq() 接收
  5. iq_to_mac() 解码比较
  6. 统计 BER/PER
```

注意事项:
- 首次使用线缆回环 + 衰减器，排除空口干扰
- 频偏校准: E310 TCXO 精度 ±2.5 ppm，对应 ±6 kHz@2.4 GHz
- AGC: 初始固定增益，后续增加自动增益控制

### 阶段三: 双机通信

**目标**: 两台 E310 之间收发通信

```
scripts/tx_node.py:    # G 节点发射端
scripts/rx_node.py:    # T 节点接收端

流程:
  G 节点:
    1. 广播帧发射 (周期性)
    2. 等待接入请求
    3. 建立连接
    4. 数据帧发射

  T 节点:
    1. 扫描广播帧
    2. 前导码检测 + 同步
    3. 解码广播帧
    4. 发送接入请求
    5. 数据帧接收
```

### 阶段四: 协议栈集成

**目标**: MAC 状态机驱动的完整协议栈运行

```
main.py (重构):
  - CLI 入口: tx/rx/loopback/sim 模式选择
  - G 节点模式: 广播 → 接入 → 连接 → 数据传输
  - T 节点模式: 扫描 → 接入 → 连接 → 数据接收
  - 仿真模式: 各 Phase 仿真

关键集成:
  LinkManager → mac_to_iq() → SLETransceiver → 空口 → iq_to_mac() → LinkManager
```

### 阶段五: 性能优化

- 实时性: NumPy 计算耗时评估，瓶颈识别
- 缓冲区管理: TX/RX 缓冲区大小调优
- 跳频延迟: `tune_channel()` 切换时间测量
- 功率控制: TX 增益动态调节
- 时钟同步: PPS/GPS 外部参考时钟 (可选)

---

## 优先级排序

以下按实现价值和部署依赖排序:

| 优先级 | 任务 | 依赖 |
|--------|------|------|
| P0 | 广播帧子信息补全 (7.1.4.1/2/4/5/6) | 接入流程前置 |
| P0 | 接入流程实现 (7.1.3) | 双机通信前置 |
| P1 | 时隙调度子系统 | 实时硬件运行前置 |
| P1 | 硬件验证脚本 | E310 硬件就绪 |
| P2 | 控制面流程补全 (7.2.1-14) | 连接态功能 |
| P2 | 休眠唤醒管理 | 低功耗运行 |
| P3 | 物理层回环测试 | 硬件验证完成 |
| P3 | 双机通信脚本 | 接入流程 + 硬件验证 |
| P4 | 协议栈 CLI 集成 | 全部基础功能 |
| P5 | 性能优化 | 双机通信调通 |
