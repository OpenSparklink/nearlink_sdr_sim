[project]
name = "nearlink-sdr"
version = "1.0.4"
description = "SparkLink SLE PHY/MAC link-level simulation conforming to TXS-10002-2025"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.3",
    "scipy>=1.17.1",
    "attrs>=24.3.0",
    "cattrs>=23.1.2",
    "cryptography>=45.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ruff>=0.15.6",
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    {include-group = "docs"},
]
docs = [
    "sphinx>=8.0,<9.0",
    "myst-parser>=4.0",
    "furo>=2024.0",
    "sphinx-autodoc2>=0.5",
    "furo>=2025.12.19",
    "sphinxcontrib-mermaid>=1.0",
    "sphinx-intl>=2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/nearlink_sdr"]

[tool.ruff]
target-version = "py314"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "SIM",  # flake8-simplify
    "RUF",  # ruff-specific
]
ignore = [
    "E501",   # 行长度由 line-length 控制
    "RUF001", # 允许中文全角标点 (字符串中)
    "RUF002", # 允许中文全角标点
    "RUF003", # 允许中文注释中的 CJK 字符
    "B023",   # 允许闭包中引用循环变量 (仿真代码常用)
]

[tool.ruff.lint.per-file-ignores]
"examples/*.py" = ["F821", "E402"]

[tool.ruff.lint.isort]
known-first-party = ["nearlink_sdr"]

[tool.esbonio.sphinx]
pythonCommand = ["uv", "run", "--group", "docs", "python"]
