{
  "name": "nearlink-sdr",
  "private": true,
  "description": "Node.js tooling for nearlink-sdr docs (markdownlint + mermaid-cli)",
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.0.0",
    "markdownlint-cli2": "^0.17.0"
  }
}
